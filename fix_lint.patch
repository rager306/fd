--- api/queue/worker_test.go
+++ api/queue/worker_test.go
@@ -60,7 +60,7 @@
	defer cancel()

	store := NewResultStore()
-	defer store.Close()
+	defer func() { _ = store.Close() }()

	worker := NewWorker(WorkerConfig{
		BatchSize:     2,
@@ -92,7 +92,7 @@
	defer cancel()

	store := NewResultStore()
-	defer store.Close()
+	defer func() { _ = store.Close() }()

	worker := NewWorker(WorkerConfig{
		BatchSize:     2,
@@ -129,7 +129,7 @@
	defer cancel()

	store := NewResultStore()
-	defer store.Close()
+	defer func() { _ = store.Close() }()

	worker := NewWorker(WorkerConfig{
		BatchSize:     2,
@@ -162,7 +162,7 @@
	defer cancel()

	store := NewResultStore()
-	defer store.Close()
+	defer func() { _ = store.Close() }()

	worker := NewWorker(WorkerConfig{
		BatchSize:     2,
@@ -205,7 +205,7 @@
	defer cancel()

	store := NewResultStore()
-	defer store.Close()
+	defer func() { _ = store.Close() }()

	worker := NewWorker(WorkerConfig{
		BatchSize:     2,
--- api/main_test.go
+++ api/main_test.go
@@ -274,7 +274,7 @@
	if err != nil {
		t.Fatalf("failed to dial container: %v", err)
	}
-	defer c.Close()
+	defer func() { _ = c.Close() }()

	out, err := io.ReadAll(c)
	if err != nil {
@@ -286,7 +286,7 @@
	if err != nil {
		t.Fatalf("failed to dial live endpoint: %v", err)
	}
-	defer c.Close()
+	defer func() { _ = c.Close() }()
 }

 func findFreePort() (int, error) {
--- api/main.go
+++ api/main.go
@@ -456,7 +456,7 @@
	}
	if opts.QueueCapacity > 0 {
		resultStore := queue.NewResultStore()
-		defer resultStore.Close()
+		defer func() { _ = resultStore.Close() }()

		requestQueue = make(chan queue.Item, opts.QueueCapacity)
		batchSize := envutil.Int("QUEUE_BATCH_SIZE", 32)
--- api/fd_v2_queue_integration_test.go
+++ api/fd_v2_queue_integration_test.go
@@ -34,7 +34,7 @@
	gin.SetMode(gin.TestMode)

	store := queue.NewResultStore()
-	t.Cleanup(func() { store.Close() })
+	t.Cleanup(func() { _ = store.Close() })

	requestQueue := make(chan queue.Item, queueCap)

--- api/embed/coalescedembedder_test.go
+++ api/embed/coalescedembedder_test.go
@@ -83,7 +83,7 @@
		go func() {
			defer wg.Done()
			time.Sleep(10 * time.Millisecond) // Ensure it lands after the first batch starts
-			co.Embed(context.Background(), []string{"x"})
+			_, _ = co.Embed(context.Background(), []string{"x"})
		}()

		// Give the main coalescer time to wait in the background loader loop
