--- api/cache/redis.go
+++ api/cache/redis.go
@@ -12,6 +12,7 @@
	"strconv"
	"strings"
	"time"
+	"unsafe"

	"github.com/redis/go-redis/v9"
 )
@@ -239,8 +240,14 @@
 // component for the embedding text (the dim and prefix are added by
 // the key() method to form the full Redis key).
 func (c *RedisCache) HashText(text string) string {
-	h := sha256.Sum256([]byte(text))
-	return hex.EncodeToString(h[:])
+	var textBytes []byte
+	if text != "" {
+		textBytes = unsafe.Slice(unsafe.StringData(text), len(text)) //nolint:gosec // G103: performance optimization for byte casting
+	}
+	h := sha256.Sum256(textBytes)
+	var buf [64]byte
+	hex.Encode(buf[:], h[:])
+	return string(buf[:])
 }

 // Get retrieves the cached embedding vector for (text, dim). Returns
