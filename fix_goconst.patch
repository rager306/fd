--- api/embed/types.go
+++ api/embed/types.go
@@ -3,6 +3,9 @@
 import (
	"encoding/json"
	"fmt"
 )

+const ObjectTypeList = "list"
+
 const (
	// EncodingFormatFloat indicates embeddings should be returned as []float32.
	EncodingFormatFloat = "float"
--- api/handlers/embeddings.go
+++ api/handlers/embeddings.go
@@ -262,7 +262,7 @@
	}

	return embed.EmbeddingsResponse{
-		Object: "list",
+		Object: embed.ObjectTypeList,
		Data:   data,
		Model:  modelID,
		Usage: embed.Usage{
--- api/handlers/embeddings_integration_test.go
+++ api/handlers/embeddings_integration_test.go
@@ -176,7 +176,7 @@
				if err := json.Unmarshal(w.Body.Bytes(), &resp); err != nil {
					t.Fatalf("unmarshal: %v\nbody: %s", err, w.Body.String())
				}
-				if resp.Object != "list" {
+				if resp.Object != embed.ObjectTypeList {
					t.Errorf("expected object list, got %q", resp.Object)
				}
				if len(resp.Data) != tt.expectedLen {
--- api/handlers/queue_handlers.go
+++ api/handlers/queue_handlers.go
@@ -124,7 +124,7 @@
		c.JSON(http.StatusOK, result.Data)
	case queue.StatusSuccess:
		resp := embed.EmbeddingsResponse{
-			Object: "list",
+			Object: embed.ObjectTypeList,
			Data:   result.Data,
			Model:  h.modelID,
			Usage: embed.Usage{
