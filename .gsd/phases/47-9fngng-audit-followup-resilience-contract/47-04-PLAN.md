# S04: Warmup retry and closure

**Goal:** Add bounded warmup retry/backoff, validate R034, produce issue #6 closure matrix, run final gates, validate and complete M047.
**Demo:** Warmup transient failures retry and the milestone closes issue #6 with final gates and closure matrix.

## Must-Haves

- Warmup retries after transient failure and marks ready after later success.
- Warmup terminal failure records last error with attempt context.
- Issue #6 closure artifact covers #11, #14, #13, #32, #25, #15.
- R034 is validated and final gates pass.
- Milestone validation and completion artifacts are written.

## Proof Level

- This slice proves: Deterministic warmup retry tests, full Go tests, lint, govulncheck, artifact UAT, milestone validation.

## Integration Closure

Readiness remains false until successful warmup; prior S01-S03 guarantees stay green.

## Verification

- Warmup logs include attempt counts and terminal failure context.

<tasks>
- [x] **T01**: Pinned warmup retry behavior with red tests. _(medium)_
  Add tests proving warmup can fail once then retry and mark ready, and bounded terminal failure records last error. Use injectable retry policy with zero delay for deterministic tests.
  - Files: `api/main.go`, `api/main_test.go`
  - Verify: cd api && go test ./... (expected red before implementation).
- [x] **T02**: Added bounded warmup retry with deterministic tests. _(medium)_
  Add a small warmup retry policy and update `startModelWarmup` to retry failed prewarm attempts with bounded backoff. Clear readiness error on success via existing lifecycle state behavior and record terminal errors after max attempts.
  - Files: `api/main.go`, `api/main_test.go`
  - Verify: cd api && go test ./...
- [x] **T03**: Ran final M047 gates, wrote issue #6 closure matrix, and validated R034. _(medium)_
  Write S04 and milestone closure artifacts, validate R034, run full tests, race cache smoke if relevant, lint, govulncheck, UAT checks, milestone validation, and completion.
  - Files: `benchmark-results/m047-s04-warmup-retry-closure.md`, `benchmark-results/m047-issue-6-closure.md`, `.gsd/REQUIREMENTS.md`
  - Verify: cd api && go test ./...; golangci-lint; govulncheck; artifact UAT; milestone validation.
</tasks>

## Files Likely Touched

- api/main.go
- api/main_test.go
- benchmark-results/m047-s04-warmup-retry-closure.md
- benchmark-results/m047-issue-6-closure.md
- .gsd/REQUIREMENTS.md
