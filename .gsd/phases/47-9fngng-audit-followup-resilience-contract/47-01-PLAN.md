# S01: Contract cleanup baseline

**Goal:** Validate issue #6 against current code, then fix the small contract findings: safe env integer parsing and the registered-but-never-emitted error code policy.
**Demo:** Issue #6 small contract findings have red tests and fixes for env integer parsing and error code registry policy.

## Must-Haves

- `documents/issue-6-current-m047.md` is tracked as the issue input artifact.
- Red tests demonstrate `getEnvInt` overflow/negative values currently do not safely fall back.
- Error registry policy is enforced by tests.
- Code fixes are minimal and preserve existing emitted error behavior.
- R036 is validated.

## Proof Level

- This slice proves: TDD unit tests plus `cd api && go test ./...` for the slice.

## Integration Closure

No endpoint behavior changes except removing or documenting un-emitted error codes; `FD_MAX_IN_FLIGHT` default semantics remain unchanged.

## Verification

- S01 produces the baseline issue validation artifact for downstream slices.

<tasks>
- [x] **T01**: Pinned S01 issue #6 contract failures with red tests. _(small)_
  Use issue #6 and current code to pin S01 scope. Add tests showing safe fallback expectations for `getEnvInt` overflow/negative values and registry expectations for canonical error codes.
  - Files: `api/main_env_test.go`, `api/handlers/errors_test.go`, `documents/issue-6-current-m047.md`
  - Verify: cd api && go test ./... (expected red before fixes), then focused failing tests are recorded.
- [x] **T02**: Fixed safe env integer parsing and removed un-emitted error codes from the public registry. _(medium)_
  Replace hand-rolled `getEnvInt` parsing with safe `strconv.Atoi` behavior that falls back on invalid, overflow, and negative values. Resolve the error-code registry contract by removing un-emitted reserved codes or adding explicit reserved metadata with test coverage; prefer removal unless current docs/tests require them.
  - Files: `api/main.go`, `api/handlers/errors.go`, `api/handlers/errors_test.go`, `api/main_env_test.go`
  - Verify: cd api && go test ./...
- [x] **T03**: Recorded S01 evidence and validated R036. _(small)_
  Write a short S01 evidence artifact, validate R036, run focused/full tests, and complete S01.
  - Files: `benchmark-results/m047-s01-contract-cleanup.md`, `.gsd/REQUIREMENTS.md`
  - Verify: cd api && go test ./... plus static artifact check.
</tasks>

## Files Likely Touched

- api/main_env_test.go
- api/handlers/errors_test.go
- documents/issue-6-current-m047.md
- api/main.go
- api/handlers/errors.go
- benchmark-results/m047-s01-contract-cleanup.md
- .gsd/REQUIREMENTS.md
