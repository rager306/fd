# S01: Warmup recovery contract — UAT

**Milestone:** M051-h1xr44
**Written:** 2026-07-01T10:50:21.942Z

## UAT Type
- UAT mode: mixed

## UAT Checks

### UAT-01: Startup warmup policy expanded (env-configurable)
**Verdict: PASS**
Evidence: docker logs fd_api show "model warmup policy max_attempts:5 backoff:5s". Code: api/main.go warmupRetryPolicyFromEnv(). Existing TestStartModelWarmup* pass (4 tests, they pass policy explicitly).

### UAT-02: Periodic recovery goroutine auto-recovers after startup failure
**Verdict: PASS**
Evidence: .gsd/runtime/M051-h1xr44/api-recovery-logs.jsonl — startup 5 attempts failed (20s) → recovery attempt 1-6 failed every 30s → TEI Ready (~3 min) → "warmup recovery succeeded attempt:7 latency_ms:197 elapsed_ms:210197" → /health ok. Confirmed in two independent runs.

### UAT-03: Recovery stops deterministically on terminal conditions
**Verdict: PASS**
Evidence: api/lifecycle/recovery_test.go passes with -race: TestStartWarmupRecoveryStopsAtSuccess (attempts=1, no growth), StopsAtShutdown (BeginShutdown→growth stops), StopsAtContextCancel (ctx.cancel→stops), AlreadyWarmNoop, DisabledIsNoop.

### UAT-04: Feature flag rollback works
**Verdict: PASS**
Evidence: TestStartWarmupRecoveryDisabledIsNoop — enabled=false → no model call. Env knob FD_WARMUP_RECOVERY_ENABLED default=true (opt-out via BoolOrDefault).

### UAT-05: No regression in /health shape or middleware
**Verdict: PASS**
Evidence: /health response shape unchanged. LifecycleGateWithCapacity behavior unchanged (503 model_not_loaded Retry-After:5 during degraded, 200 after recovery). go test ./api/... -race passes all 11 packages.

### UAT-06: No modification of HIGH-impact State methods
**Verdict: PASS**
Evidence: gitnexus_impact on MarkWarmupDone was HIGH (2 callers). Recovery code calls existing State methods as-is — no signature changes. api/lifecycle/state.go untouched.
