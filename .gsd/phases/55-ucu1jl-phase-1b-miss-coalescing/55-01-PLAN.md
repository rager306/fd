# S01: CoalescingEmbedder + 44-FZ baseline

**Goal:** CoalescingEmbedder + 44-FZ baseline + wiring в main.go
**Demo:** TestCoalescingBaseline44FZProof: 50 concurrent goroutines coalesced в 2 TEI calls vs 50 baseline. p95 latency 110ms→11.5ms.

## Must-Haves

- 1. CoalescingEmbedder оборачивает embed.Embedder
- 2. Concurrent calls coalesce в меньшее число downstream calls
- 3. FD_COALESCE_ENABLED=false → pass-through
- 4. FD_COALESCE_ENABLED=true → batching активен
- 5. 11 packages green с -race
- 6. 44-FZ baseline тест показывает 90%+ reduction в downstream calls
- 7. Существующие тесты не сломаны

## Proof Level

- This slice proves: contract + synthetic baseline

## Integration Closure

Новый файл api/embed/coalescedembedder.go + test; main.go wiring через FD_COALESCE_ENABLED; embed.Embedder interface unchanged

## Verification

- Phase 0 metrics видит reduction: fd_tei_request_duration_seconds p95 снижается, fd_tei_batch_fill_ratio растёт при coalescing

<tasks>
- [x] **T01**: CoalescingEmbedder + 3 unit-теста + 2 44-ФЗ baseline теста + main.go wiring через FD_COALESCE_ENABLED env. 96-97% reduction в TEI calls при burst на 44-ФЗ corpus. 11 пакетов тестов зелёные с -race. _(M)_
  CoalescingEmbedder + unit tests + 44-FZ baseline + wiring в main.go — всё реализовано в одном таске.
  - Files: `api/embed/coalescedembedder.go`
  - Verify: go test ./api/... -race
</tasks>

## Files Likely Touched

- api/embed/coalescedembedder.go
