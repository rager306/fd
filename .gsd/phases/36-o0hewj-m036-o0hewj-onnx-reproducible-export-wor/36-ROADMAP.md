# M036-o0hewj: M036-o0hewj: ONNX reproducible export workflow contract

**Vision:** Define a truthful no-upload route for resolving the ONNX model source blocker through regenerated-export proof.

## Success Criteria

- No-upload reproducible-export alternative is actionable and truthful.
- No byte-for-byte regenerated proof is claimed without running regeneration.
- Exact-binary hosting remains available as separate path but still blocked until upload/mirror approval.
- TEI remains production/default and ONNX remains opt-in experimental.

## Slices

- [x] **S01: S01** `risk:medium` `depends:[]`
  > After this: After this, the repo has a reproducible-export workflow contract that names pinned inputs, expected outputs, and gates before regenerated ONNX can replace exact-binary hosting.

- [x] **S02: S02** `risk:low` `depends:[]`
  > After this: After this, M036 is verified, summarized, committed locally, and reindexed.

## Boundary Map

## Boundary Map

Not provided.
