# M025-9bvjxa: M025-9bvjxa: ONNX artifact provisioning and hosted CI contract

**Vision:** Turn the locally proven ONNX 1024 Docker path into a truthful artifact provisioning, diagnostics, and hosted CI contract without committing binaries or promoting ONNX to default.

## Success Criteria

- Artifact provisioning/cache contract is explicit and checksum-based.
- Operational diagnostics/rollout contract is explicit and preserves TEI default.
- Hosted full ONNX CI path is safe/manual until artifact source exists.
- No binary artifacts are committed.
- Verification passes and GitNexus scope is low/clean.

## Slices

- [x] **S01: S01** `risk:high` `depends:[]`
  > After this: After this, there is a concrete provisioning contract and local automation for checksum-verified artifact staging without git-tracking binaries.

- [x] **S02: S02** `risk:medium` `depends:[]`
  > After this: After this, there is an explicit operational diagnostics and rollout/rollback contract for ONNX opt-in deployments.

- [x] **S03: S03** `risk:medium` `depends:[]`
  > After this: After this, hosted CI has a safe manual ONNX packaging workflow skeleton that runs only when artifact inputs are supplied, plus normal default CI remains unaffected.

## Boundary Map

## Boundary Map

Not provided.
