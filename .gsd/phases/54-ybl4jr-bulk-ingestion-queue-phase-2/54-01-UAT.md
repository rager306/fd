# S01: Bulk ingestion queue MVP — UAT

**Milestone:** M054-ybl4jr
**Written:** 2026-07-03T11:28:15.493Z

## UAT Type
- UAT mode: artifact-driven

## UAT Checks

**UAT-01: FD_QUEUE_ENABLED=false → endpoints 404 (backward compat)**
Verdict: PASS. main.go wiring: `if queueEnabled { r.POST("/v1/queue", ...) }` — feature flag absent = no endpoints registered = 404 from gin default router.

**UAT-02: FD_QUEUE_ENABLED=true → POST /v1/queue returns 202 + X-Request-Id**
Verdict: PASS. TestQueueSubmitAndPollCompleted: submit returns 202 with X-Request-Id header, polling returns 200 with embeddings once processed.

**UAT-03: Invalid input → 400 error envelope**
Verdict: PASS. TestQueueRejectsInvalidInput: empty input body returns 400.

**UAT-04: Backpressure → 503 + Retry-After when queue full**
Verdict: PASS. handlers/queue_handlers.go: `select { case h.items <- item: ... default: c.Header("Retry-After", "5"); WriteError(...) }`. TestQueueBackpressureRejectsWhenFull verifies.

**UAT-05: Time-windowed batching → 1 TEI call for N items in window**
Verdict: PASS. TestWorkerBatchesMultipleItems: 3 items submitted → emb.calls=1, batch_size=3.

**UAT-06: maxBatchSize cap enforced**
Verdict: PASS. TestWorkerRespectsMaxBatchSize: 8 items + maxBatchSize=4 → emb.calls=2.

**UAT-07: Graceful shutdown marks in-flight items failed**
Verdict: PASS. TestWorkerStopsOnContextCancel: cancel ctx → StatusFailed returned to items.

**UAT-08: 404 for unknown poll ID**
Verdict: PASS. TestQueuePollReturns404ForUnknownId: GET /v1/queue/nonexistent → 404.

**UAT-09: 5 new queue metrics exposed at /metrics**
Verdict: PASS. fd_queue_depth, fd_queue_drain_total, fd_queue_submit_total, fd_queue_batch_size, fd_queue_process_duration_seconds all registered.

**UAT-10: All existing tests still pass (no regression)**
Verdict: PASS. go test ./api/... -race — 11 packages green.
