# S01: L1 cache capacity + TTL env-config — UAT

**Milestone:** M053-ckvgjw
**Written:** 2026-07-03T10:46:29.603Z

## UAT Type
- UAT mode: artifact-driven

## UAT Checks

**UAT-01: Env defaults work without override**
Verdict: PASS. Evidence: TestCacheConfigDefaultsWithoutEnv confirms FD_CACHE_MAX_SIZE defaults=10000, FD_CACHE_LOCAL_TTL defaults=30s. Docker log: "cache configured l1_max_entries=10000 l1_ttl=30s".

**UAT-02: Env override applies correctly**
Verdict: PASS. TestCacheConfigFromEnv: FD_CACHE_MAX_SIZE=500 → localCache size=500. TestDurationOrDefaultValidDurations: "5m", "60s", "100ms", "0s" all parse correctly.

**UAT-03: Invalid env values fall back to defaults**
Verdict: PASS. TestDurationOrDefaultInvalidFallsBack: "30", "notaduration", "-5s", "abc" → fallback to 10s. envutil.PositiveInt: negative values → fallback to default.

**UAT-04: Cache size eviction works**
Verdict: PASS. TestLocalCacheWithSmallSize — maxSize=5 with 10 entries → size ≤5. existing cache tests confirm eviction in LocalCache.

**UAT-05: Unlimited mode works**
Verdict: PASS. TestLocalCacheWithZeroSize — maxSize=0, 20 entries → all accommodated, size=20.

**UAT-06: README documents new env knobs**
Verdict: PASS. README "Redis and cache" table содержит FD_CACHE_MAX_SIZE и FD_CACHE_LOCAL_TTL с sizing formula guidance.

**UAT-07: No regression in existing tests**
Verdict: PASS. go test ./api/... -race — 10 packages green.
