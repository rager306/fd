# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M005-pyn4x7 S02 | runtime-performance | How to handle TEI ONNX-missing fallback warnings for deepvk/USER-bge-m3 | Keep the current measured TEI CPU/Candle fallback runtime as acceptable for now; treat ONNX export/runtime changes as a future measured optimization that must be validated by A/B benchmarks before becoming default. | M003/M004 live validation showed the stack is correct and performant enough with the current runtime, while TEI logs indicate ONNX artifacts are missing. Exporting ONNX adds artifact and deployment complexity, so it should be justified by measured cold-latency/memory gains rather than treated as an immediate correctness fix. | Yes | agent |
| D002 | M008-6hnowu | embedding-runtime-optimization | Scope embedding optimization around model-preserving runtime changes, not arbitrary embedding model replacement. | Optimize runtime for the current Russian/legal-capable embedding model first; treat model substitution as a separate research path requiring Russian legal corpus quality benchmarks. | The user clarified that Russian language support and legal-corpus suitability are mandatory. Faster alternative models are not acceptable unless they prove quality on the target domain. | Yes, if a candidate model passes agreed Russian legal corpus benchmarks. | human |
| D003 | M008-6hnowu | redis-cache-architecture | Embedding cache retention policy for research workflows | Treat Redis L2 as a long-lived reusable embedding cache for research/chunking workloads, with configurable retention and model/version-aware invalidation rather than fixed short-lived transient caching. | The user clarified that research may run chunking and reuse vectors multiple times, so cache retention should reduce repeated model load across experiments. | Yes, based on memory pressure, corpus size, and production deployment constraints. | human |
| D004 | M008-6hnowu | operability-configuration | Configuration surface for embedding and Redis optimization tuning | Expose tunable embedding/cache/runtime parameters through environment variables, grouped by correctness-affecting cache namespace settings and runtime-only performance settings, with safe defaults and validation. | The user wants fine tuning during research and deployment. Env-based configuration allows iteration without source changes, but correctness-affecting values must be explicit and validated to avoid stale vector reuse. | Yes, after the implementation spike reveals which knobs are useful and which should stay internal. | human |
| D005 | M008-6hnowu | benchmarking-observability | Benchmark comparability requires recording effective env/config parameters | Every benchmark run should persist a sanitized effective configuration snapshot alongside results, including env-derived tuning parameters and git/runtime identifiers, while excluding secrets. | The user clarified that benchmark results must be tied to the .env parameters used during the run; otherwise optimization comparisons can be misleading. | Yes, the exact list of recorded fields can evolve as new tuning knobs are added. | human |
| D006 |  | runtime | Whether M010 evidence is sufficient to switch fd's production embedding runtime from TEI to ONNX. | Treat FP32 dense-only ONNX for `deepvk/USER-bge-m3` as locally feasible, but keep TEI as the production/default runtime until a separate non-default adapter, performance benchmark, artifact distribution plan, and Russian/legal quality gate are completed. | M010 proved exact-model FP32 dense ONNX export/load and TEI cosine agreement on fixed probes, but did not prove production throughput, larger Russian/legal retrieval quality, artifact operations, or service integration behavior. A default runtime switch would be premature; a gated opt-in prototype is the safe next step. | Yes | agent |
| D007 | After M011-33b7wf completion | runtime-optimization | Next ONNX work gate after M011 tokenizer mismatch | Do tokenizer parity before any further ONNX performance benchmark or production recommendation. | M011 proved Go ONNX Runtime can load/run, but isolated-cache TEI-vs-Go-ONNX comparison failed because Go tokenization diverged from Hugging Face tokenization. Throughput benchmarks before token-level parity would measure a different embedding function and mislead optimization decisions. | Yes, if a future architecture intentionally uses a sidecar or other tokenizer path with equivalent output evidence. | collaborative |
| D008 | Start of M013-nhu1x9 after M012 completion | runtime-optimization | How to proceed after M012 proved tokenizer parity via HF Rust bindings | Package the HF Rust tokenizer dependency behind an explicit opt-in build-tag/native-artifact path before runtime integration or ONNX performance benchmarking. | M012 proved `daulet/tokenizers` plus `libtokenizers.a` matches Hugging Face tokenization, while the current `sugarme/tokenizer` path fails. Importing the native binding into default builds without packaging/build tags could break TEI/default CI and Docker builds, so packaging must be solved first. | Yes, if sidecar tokenization is chosen instead of native binding after packaging evaluation. | collaborative |
| D009 | Start of M014-vjfs9f after M013 completion | benchmarking | Next step after tagged ONNX cosine equivalence | Run a tagged ONNX performance benchmark milestone before any production/default runtime decision. | M013 proved fixed-probe semantic equivalence for the tagged `hf_tokenizers` ONNX path. Performance, startup, memory, cache behavior, and larger operational constraints are still unknown; benchmarking is now meaningful but production switch remains premature. | Yes, if benchmark evidence shows tagged ONNX is not competitive or operationally too complex. | collaborative |
| D010 | M014-vjfs9f S04 benchmark synthesis | runtime-performance | Tagged ONNX performance benchmark recommendation | Continue ONNX as an opt-in experimental path; do not switch production/default from TEI yet. | M014 shows tagged ONNX is materially faster on cold/model-bound paths and slightly higher in peak throughput in one local run, but cache-dominated paths are mixed, startup/RSS and native packaging are operationally significant, correctness is fixed-probe rather than full Russian/legal retrieval quality, and Docker/CI artifact packaging is not solved. | Yes, after repeated benchmarks, Docker/CI native artifact packaging, and broader Russian/legal quality evaluation. | agent |
| D011 | Start of M015-22msl0 | quality-gate | M015 corpus basis for ONNX quality gate | Use the new 44-FZ JSONL file as the first Russian/legal retrieval parity corpus for TEI-vs-tagged-ONNX quality gating. | The user provided `tests/44-FZ-2026-articles.jsonl` specifically for Russian/legal quality testing. The file contains structured legal articles/parts/clauses but no explicit qrels, so the first gate should measure TEI-vs-ONNX retrieval parity and synthetic known-item retrieval rather than claiming broad absolute legal relevance quality. | Yes, if explicit query/qrel labels are later added. | human |
| D012 | M015-22msl0 S04 legal quality verdict | quality-gate | Tagged ONNX Russian/legal quality gate outcome | Block ONNX packaging/tuning as the next priority; investigate long-text legal quality divergence first while keeping TEI as production/default. | M015 live evaluation on the user-provided 44-ФЗ corpus failed strict cross-backend cosine equivalence. Ranking parity was mostly strong, but document cosine min was 0.369 and query cosine min was 0.657, indicating severe outliers on legal text. The current ONNX path uses max sequence length 128, so long-text/truncation behavior must be investigated before packaging or tuning. | Yes, after a corrected long-text/chunking or longer-sequence ONNX path passes the legal quality gate. | agent |
| D013 | Start of M016-pdcjat | quality-gate | Next priority after M015 legal quality gate failure | Investigate ONNX long-text legal divergence first, and add a parallel research track for alternative Russian/legal-capable embedding models to benchmark later. | The user accepted the M015 conclusion that packaging/tuning is premature and explicitly requested adding alternative-model research. M015 showed severe long-text vector divergence for tagged ONNX despite mostly strong ranking parity, so root-cause investigation remains the primary gate; model alternatives should be researched but not adopted without legal-corpus evidence. | Yes, after long-text divergence is explained and candidate models have comparable legal-corpus benchmark data. | human |
| D014 | M016-pdcjat S03 | onnx-legal-remediation | How to remediate M015/M016 ONNX legal divergence after confirming 128-token truncation as the main failure cause. | Keep TEI as production/default and make the next ONNX gate a 512-token quality remediation plus long-text chunking/longer-sequence policy before packaging, tuning, or promotion. | M016 S01 showed 17/17 worst legal divergence cases truncate at 128 tokens and only 2/17 at 512. M016 S02 showed local ONNX mean cosine against TEI improves from 0.9204953 at 128 to 0.99885631 at 512, while two >512 cases still need chunking or longer-sequence handling. This evidence supports quality-first remediation, not model switching or production promotion. | Yes, after a full legal retrieval gate, tagged native build checks, packaging checks, and performance benchmarks pass. | agent |
| D015 | M017-j10hmp S02 | onnx-512-quality-gate | How to interpret the M017 tagged Go ONNX 512 legal quality gate outcome. | Treat 512-token ONNX as necessary but insufficient; next ONNX remediation must add deterministic chunking or longer-sequence handling for legal fragments above 512 tokens before any production promotion. | M017 S01 improved ranking parity to top1_agreement 1.0, mean_overlap_at_5 0.997701, and ONNX recall ratio 1.0, but the strict cross-backend cosine gate still failed with minimum 0.98982302 below the 0.999 threshold. The remaining worst cases are long legal fragments above 512 tokens, so 512 alone is not enough for strict legal equivalence. | Yes, after a chunking or longer-sequence implementation reruns the full legal retrieval gate and passes or establishes an explicit threshold decision. | agent |
| D016 | M018-vq2ttb S02 | onnx-1024-quality-gate | How to interpret the M018 tagged Go ONNX 1024 legal quality gate outcome. | Treat tagged Go ONNX with max sequence length 1024 as passing the selected Russian/legal quality gate, but keep it experimental until performance, memory, artifact packaging, CI, and operational gates pass. | M018 S01 passed the full legal retrieval gate with minimum cross-backend cosine 0.99989883, top1 agreement 1.0, mean overlap@5 0.997701, and ONNX recall ratio 1.0. This retires the selected-corpus quality blocker for the 1024 path, but does not prove production readiness or performance viability. | Yes, after performance, packaging, CI, operational, and broader/unbounded-document quality gates are measured. | agent |
| D017 | M019-opzh2g S02 | onnx-1024-performance | How to interpret the M019 ONNX 1024 performance benchmark outcome. | Treat ONNX 1024 as locally performance-viable after quality pass, and move the next gate to artifact contract, Docker/CI packaging, and operational validation while keeping TEI as default. | M019 measured ONNX 1024 with best cold latency 8.3ms, warm latency mean 1.19ms, max throughput about 858 req/s, Redis L2 restart 2.02ms, batch L1 p95 4.09ms, batch L2 p95 6.70ms, and chunk reuse warm p95 6.51ms. This is competitive with or faster than the M014 TEI baseline for the measured workload, but does not validate packaging or production operations. | Yes, after packaged Docker/CI benchmarks or production-like load tests show different performance or operational behavior. | agent |
| D018 | M020-mvkq4d S02 | onnx-1024-artifact-contract | How to represent the ONNX 1024 runtime contract after quality and local performance gates passed. | Use the tracked `user-bge-m3-dense-fp32` manifest as an experimental ONNX 1024 runtime contract with quality/performance evidence, while keeping `production_default=false` and requiring Docker/CI packaging plus artifact provisioning gates before promotion. | The ONNX binary is unchanged and exported with dynamic sequence axes, so a separate binary manifest would imply a new artifact that does not exist. Updating the existing manifest preserves export provenance (`sequence_length=128`) while documenting validated runtime max sequence length 1024, M018 quality PASS, M019 local performance viability, and remaining production gates. | Yes, if a new 1024-specific exported binary or packaged artifact is produced with its own checksum and distribution path. | agent |
| D019 | M021-4t2wpt S02 | onnx-docker-ci-packaging | How to package ONNX 1024 without breaking the default Docker/CI path. | Keep the default Docker/CI build CGO-disabled and TEI-oriented; require explicit `onnx` plus `hf_tokenizers` build tags for the opt-in ONNX native-tokenizer runtime, with artifacts verified by `tools/verify_onnx_artifacts.py` before tagged ONNX use. | M021's first Docker build showed the default path was accidentally importing `onnxruntime_go`, which fails CGO-disabled Docker builds. Moving the real ONNX embedder behind an explicit `onnx` build tag restored the default image build while preserving opt-in ONNX runtime capability when native artifacts are provisioned and verified. | Yes, if a future packaging milestone creates a dedicated ONNX image target with provisioned artifacts and CI checks. | agent |
| D020 | M022-i079tk S02 | onnx-ci-packaging | How should CI handle ONNX 1024 packaging after local Docker proof? | Run only artifact-free ONNX contract checks in the default Go Quality workflow; defer full ONNX image CI until an external artifact provisioning/cache path exists. | The ONNX model, native tokenizer library, tokenizer JSON, and ONNX Runtime shared library are intentionally untracked and only available locally today. CI can truthfully validate manifest shape and binary hygiene now, but a real image build requires a provisioned artifact source to avoid fake readiness claims or committing large binaries. | Yes — once an artifact store/cache is available, CI can add full ONNX image build, tagged tests, legal quality, and performance jobs. | agent |
| D021 | M023-myx9u4 | onnx-packaged-legal-quality | What does the M023 packaged ONNX legal quality pass authorize? | Treat packaged ONNX Docker 1024 as passing the selected Russian/legal quality gate, but keep ONNX opt-in experimental until packaged performance, artifact provisioning/CI, and operational rollout gates pass. | The Docker-packaged ONNX runtime matched TEI on the selected Russian/legal corpus with minimum cosine 0.99989883, top-1 agreement 1.0, and ONNX recall ratio 1.0. However, this is quality parity evidence only; performance in the packaged image, external artifact supply, hosted CI, startup diagnostics, and rollout/rollback remain unresolved. | Yes — after packaged performance and artifact provisioning/CI gates pass, this can be revisited for production rollout planning. | agent |
| D022 | M024-b8pfpl | onnx-packaged-performance | What does the M024 packaged ONNX performance pass authorize? | Treat packaged ONNX Docker 1024 as locally performance-viable after legal quality pass, but keep ONNX opt-in experimental until artifact provisioning/CI, operational diagnostics, and rollout/rollback gates pass. | The packaged Docker image achieved best cold latency 7.6ms, warm latency mean 2.03ms, max throughput about 864 req/s, and Redis L2 restart 3.36ms, preserving performance viability relative to prior TEI and local ONNX evidence. However, hosted artifact provisioning/CI, operational diagnostics, and rollout controls remain unresolved. | Yes — revisit after artifact provisioning, hosted ONNX CI, and operational rollout gates are completed. | agent |
| D023 | M025-9bvjxa S02 | onnx-operational-rollout | How should ONNX operational rollout proceed after packaged quality and performance passes? | Keep ONNX rollout staged and opt-in with TEI as the default rollback path; do not promote ONNX until startup diagnostics, artifact provisioning/CI, security review, and rollout/rollback proof are implemented and verified. | Packaged quality and performance evidence proves the ONNX image is viable locally, but operational safety still depends on artifact preflight diagnostics, immutable provisioning, hosted CI, safe logging, and tested rollback. TEI remains the known production/default path and should remain the immediate fallback. | Yes — revisit after operational diagnostics and hosted artifact provisioning gates are implemented and verified. | agent |
| D024 | M026-ji0i9y | onnx-operational-diagnostics | What does the M026 operational diagnostics implementation authorize? | Treat M026 as implementing the first ONNX startup diagnostics gate: safe health metadata, sequence-length preflight, and safe startup/cache namespace logs are in code; production remains blocked until tokenizer/runtime-library/provider diagnostics, artifact provisioning, security review, and rollout proof pass. | The code now exposes safe ONNX runtime metadata and rejects sequence lengths beyond the manifest contract while preserving TEI/default health behavior. However, several operational preflight items remain unimplemented and production rollout still requires artifact source, hosted CI, security, and staged rollback evidence. | Yes — revisit after remaining operational diagnostics and rollout gates are implemented and verified. | agent |
| D025 | M027-qswsja | onnx-preflight-diagnostics | What does the M027 preflight diagnostics gate authorize? | M027 authorizes ONNX startup preflight hardening only: tokenizer JSON size/sha verification from manifest metadata, optional runtime library sha via ONNX_RUNTIME_SHA256, CPU-only configured-provider validation, and safe health verification flags. It does not authorize production/default promotion or claim runtime provider enumeration. | The implementation closes several operational startup gaps while preserving default TEI behavior. Runtime library sha remains optional because there is not yet a tracked immutable runtime-library source contract, and provider diagnostics are limited to the configured CPU path actually supported by the current Go ONNX startup. | Yes — revisit after runtime library source manifests, provider enumeration, security review, hosted CI, and staging rollout evidence exist. | agent |
| D026 | M028-y63tog | onnx-security-review | How should M028 ONNX security review findings affect rollout sequencing? | Treat MEDIUM-1 and MEDIUM-2 from M028 as blockers before relying on the hosted/manual ONNX packaging workflow as a trusted gate: add URL source policy/private-address blocking/byte caps and archive member size/type caps first. Keep M028 read-only and defer remediation to a separate milestone. | The review found no artifact integrity bypass, but arbitrary URL fetching and unbounded downloads/extraction can abuse CI runners or cause availability issues. These should be remediated before hosted workflow output is used as rollout evidence, while preserving the read-only audit trail. | Yes — revisit after remediation and hosted workflow proof pass. | agent |
| D027 | M029-4nh2ca | onnx-provisioning-security | How does M029 change the ONNX hosted packaging security gate? | Treat M028 MEDIUM-1 and MEDIUM-2 as remediated by M029 for the provisioning helper: remote artifact URLs are HTTPS-only, redirect-disabled, private-network-blocked by default, optionally host-allowlisted, and byte-capped; native tokenizer archive extraction now requires regular files and pre-copy size caps. M028 LOW path-output/path-root findings remain open, and hosted packaging still needs immutable sources plus a real workflow run before rollout evidence. | M029 addresses the CI/operator availability risks identified in M028 without changing runtime defaults. However, lower-severity path disclosure/path-root issues and hosted artifact source proof remain unresolved, so this improves but does not complete rollout readiness. | Yes — revisit after LOW findings, immutable source selection, and hosted workflow proof are complete. | agent |
| D028 | M030-3kdha1 | onnx-path-security | How does M030 change the ONNX security remediation status? | Treat M028 LOW-3 and LOW-4 as remediated by M030 for default ONNX artifact tooling/startup behavior: manifest artifact paths are approved-root constrained and default diagnostics avoid absolute host path disclosure where practical. Remaining rollout blockers are immutable artifact source selection, hosted workflow proof, and explicit production decision. | M030 aligns Go startup validation and Python artifact tooling around repo-relative approved roots and safer path display, closing the remaining low-severity findings from M028. This improves operational safety but still does not provide immutable external artifacts or hosted proof. | Yes — revisit after immutable source selection and hosted workflow proof. | agent |
| D029 | M031-gn517a | onnx-artifacts | How to handle ONNX artifact source selection after M030 security hardening | Select pinned checksum-matched source candidates for native tokenizer (`daulet/tokenizers` v1.27.0), tokenizer JSON (pinned `deepvk/USER-bge-m3` Hugging Face revision), and ONNX Runtime (PyPI `onnxruntime==1.26.0` CP313 manylinux wheel), while keeping the exported ONNX model binary blocked until exact immutable hosting or a separate reproducible-export gate exists. | M031 evidence found non-secret pinned sources matching existing checksums for supporting artifacts, but the actual exported ONNX binary remains local-only. Treating the upstream model files as equivalent to the exported ONNX binary would overclaim readiness and bypass required quality/performance proof. | Yes — revisit after the ONNX binary is mirrored/uploaded or a reproducible export workflow is implemented and validated. | agent |
| D030 | M032-qq6po2 | onnx-artifacts | How to treat ONNX model reproducibility/source proof after selecting supporting artifact sources | Use `tools/verify_onnx_export_contract.py` as a bounded local verifier for the existing ignored ONNX artifact contract, and keep the next gate split between exact-binary immutable hosting or a separate reproducible-export workflow with renewed legal/performance/package validation. | The verifier can prove the current local artifact matches tracked manifest, source provenance, and export metadata, but it does not regenerate the binary. Treating this as full reproducibility proof would overclaim readiness; exact hosting or full regeneration remains necessary before hosted workflow proof. | Yes — revisit after an exact immutable binary source exists or a reproducible-export workflow produces a validated artifact. | agent |
| D031 | M033-aym8ih | onnx-artifacts | How to provision ONNX Runtime from the pinned wheel source candidate | Provisioning now extracts the configured ONNX Runtime member from `.whl`/`.zip` sources using `source_contract.onnx_runtime.library_member`, size bounds, and sha verification, while preserving direct-file fallback for non-zip runtime sources. | M031 selected a PyPI wheel as the pinned source candidate, but copying the wheel directly to the runtime library path would be incorrect. Safe member extraction makes the source candidate usable without weakening archive policy or implying hosted workflow proof. | Yes — revisit if future runtime sources use a different archive format or a dedicated ONNX Runtime release tarball. | agent |
| D032 | M034-9tfz77 | onnx-ci | How the manual hosted ONNX workflow should handle ONNX Runtime checksum input | Make `onnx_runtime_sha256` an optional workflow override; when omitted, the manual ONNX packaging workflow lets provisioning use `source_contract.onnx_runtime.library_sha256` from the tracked ONNX manifest. | M033 made ONNX Runtime wheel provisioning verify against manifest metadata. Requiring duplicate workflow input for the same checksum creates unnecessary friction and can drift from the tracked source contract, while provisioning still fails if neither CLI nor manifest sha is available. | Yes — revisit if future workflow design moves artifact sources into masked secrets or a separate manifest format. | agent |
| D033 | M035-7j2h6x | onnx-artifact-source | How to represent the exact ONNX model binary source blocker before upload or reproducible export exists | Record a planned exact-binary hosting contract for the existing local ONNX artifact, including required size/sha, recommended immutable object key/release filename, allowed and forbidden source forms, and pre-dispatch requirements, while keeping `source_status=blocked` and not adding any usable `source_url`. | The project needs an actionable next source contract for hosted ONNX packaging, but adding a fake URL or marking the planned key as selected would overclaim readiness. Explicit planned_not_uploaded metadata makes the next external step clear while preserving safety and truthfulness. | Yes — revisit after the exact binary is uploaded/mirrored or a reproducible-export workflow replaces the exact-binary hosting path. | agent |
| D034 | M036-o0hewj | onnx-artifact-source | How to represent the no-upload path for resolving the ONNX model source blocker | Record a planned reproducible-export workflow contract as the no-upload alternative to exact-binary hosting, with pinned inputs, acceptance gates, and explicit `planned_not_proven` status; do not treat M032 existing-artifact verification as regenerated-export proof. | Exact-binary hosting requires an external upload/mirror action. A regenerated-export path can avoid that, but it is only truthful if the repo distinguishes planned workflow requirements from actual byte-for-byte regeneration and downstream legal/performance/package proof. | Yes — revisit after a regenerated export is actually run and the required quality/performance/package gates produce evidence. | agent |
| D035 | M037-d23oz4 | onnx-runtime-validation | How to handle Python helper evidence versus production Go or future Rust runtime validation | Treat Python export/provisioning/verifier checks as setup/provenance evidence only. Require target-runtime validation through the intended runtime before accepting or promoting any new or regenerated ONNX artifact: Go fd API/package gates for the current implementation, and independent equivalent gates for any future Rust backend. | The user correctly identified that production behavior depends on the deployed runtime, not just Python helper scripts. Python tools can drive evidence collection, but acceptance must prove the actual Go/Rust tokenizer/runtime/API/package path, including cache isolation and legal/performance gates. | Yes — revisit if production runtime changes, but each runtime still needs its own acceptance evidence. | collaborative |
| D036 | After M039 packaged Go ONNX target-runtime proof | onnx-acceptance-gates | Whether hosted GitHub Actions proof is a required ONNX gate | Hosted GitHub Actions proof is not required for ONNX acceptance or production-readiness decisions in this project. | User explicitly clarified that hosted GitHub Actions proof is not needed at all. Local and packaged runtime evidence, artifact/source provenance, Redis behavior, and rollout controls are the relevant gates; remote workflow execution should not remain a blocker by default. | Yes, only if deployment policy changes later. | human |
| D037 | M040 planning clarification after M039 packaged ONNX proof | embedding-runtime-strategy | Primary goal and boundary for fd embedding runtime work | Focus ONNX/embedding work on preparing the local fd embedding service to serve same-host services with high legal-domain quality and optimal speed; avoid experiments outside that boundary except bounded legal-domain tests of alternative embedding models if needed. | User explicitly clarified that the main goal is a service optimized for same-host consumers with excellent quality and speed, not open-ended ONNX experimentation. Alternative models are only acceptable as a fallback research path if tested on the legal domain. | Yes, if deployment target or product scope changes. | human |
| D038 | M040-pbp9z1 planning | same-host-runtime-recommendation | Basis for TEI-vs-ONNX same-host runtime recommendation | Use an evidence envelope covering legal-domain quality, same-host speed, restart/cache behavior, health/preflight clarity, and operational simplicity. | The user wants the best local embedding service for legal quality and speed, not ONNX experimentation for its own sake. A single speed metric cannot capture correctness, cache safety, or operational readiness. | Yes, if consumer workloads or deployment constraints change. | collaborative |
| D039 | M040-pbp9z1 planning | packaged-lifecycle-proof | How to prove packaged ONNX restart/cache lifecycle | Use a small scripted Docker restart command through the benchmark restart hook to prove packaged Redis L2 behavior. | M039 skipped Redis L2 restart because the container was externally managed. A scripted restart keeps benchmark comparability while avoiding a broad benchmark rewrite. | Yes, if the hook proves insufficient during M040 execution. | collaborative |
| D040 | M040-pbp9z1 planning | alternative-model-gate | How to include alternative embedding model checks without open-ended experimentation | Limit alternative embedding checks to a 1-2 candidate legal-domain quick gate and stop unless a candidate clearly challenges the current envelope. | The user allowed testing other models only at the boundary and only on legal-domain evidence. The project goal is same-host service readiness, not model search. | Yes, if the current model fails the target envelope or the user requests a dedicated model bakeoff. | collaborative |
| D041 | M040-pbp9z1/S01/T03 | api-contract | Resolve /v1/embeddings request model-field semantics | Treat /v1/embeddings request model as OpenAI-compatibility metadata; do not reject mismatches. | Existing handler tests and behavior already accept placeholder request model values while returning the configured model in the response. Hard rejection would be a breaking change for local/OpenAI-compatible clients, so the safer S01 contract is to make the response model and /health.runtime.model authoritative. | Yes | agent |
| D042 | pre-push cleanup after M040 | gsd-state-policy | Whether .gsd/gsd.db should remain tracked after the branch deletes it relative to origin/master | Do not track .gsd/gsd.db in git; treat markdown GSD artifacts and rendered project files as the portable project state, while SQLite DB files remain local runtime/cache state. | The user chose to accept deletion of .gsd/gsd.db before push. The repository already carries GSD markdown artifacts for durable context, and SQLite DB/WAL/SHM files are engine-owned local state that can drift or be regenerated/reconciled by GSD tooling. | Yes, if future GSD workflow requires committed structured DB state for reproducible automation. | human |
| D043 | Post-M043 completion before continuing active M041/M042 | planning | How to replan active M041/M042 after M043 static-analysis hardening | M041 gets a lightweight roadmap reassessment/verification contract update; M042 S02/S03 get explicit slice replans with lint-aware task constraints | M041 already has many planned lifecycle/observability/API tasks; M043 changes the definition of done more than the feature scope. M042's async and ONNX work will introduce new concurrent/runtime code, so task descriptions must directly encode small-function design, context propagation, godoc, gocyclo, gosec, and govulncheck gates. | Yes | collaborative |
| D044 | M041-4tw0w7/S02/T06 | lifecycle capacity | How fd should make F-2 model_overloaded reachable without redesigning the embedding worker model | Add operator-controlled `FD_MAX_IN_FLIGHT` lifecycle capacity gate with default 0 meaning unlimited | The fd v2 spec and R011 require `model_overloaded` + `Retry-After` behavior, but the service had no queue/worker-pool exhaustion path. A max-in-flight gate reuses lifecycle request tracking, preserves existing behavior by default, and gives operators a deterministic overload control without a larger worker-pool redesign. | Yes | agent |
| D045 | M041-4tw0w7 S04 after T04 real inference blocker assessment | performance | How to interpret fd v2 T-P latency targets for M041 S04 | Treat T-P-1..T-P-5 as cache-hot steady-state validation after service warmup and cache prefill; real cache-miss TEI CPU latency remains diagnostic and is not a launch blocker for this milestone. | Real inference tests with isolated Redis cache namespace showed fd latency matches direct TEI CPU backend latency and misses targets by a wide margin. The user explicitly chose to rescope rather than pursue backend remediation, which is not planned for this milestone. Cache HIT behavior is the deliverable controlled by fd S04 and meets the latency envelope. | Yes, if backend remediation (ONNX/GPU/faster TEI runtime) becomes in scope. | human |
